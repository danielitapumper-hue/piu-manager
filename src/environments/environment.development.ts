export const environment = {
  production: false,
  /**
   * Obfuscated Gemini API Key (XOR + Base64).
   * Generate with: node scripts/encode-api-key.js YOUR_API_KEY
   * Paste the output here.
   */
  geminiApiKey: ''
};
